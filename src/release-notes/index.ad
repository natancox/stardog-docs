= {title}
:title: Stardog Release Notes
:revnumber: {version}
:toc: right 
:toc-title: 
:toclevels: 1 
:experimental:
:page-layout: base
:description: Stardog is a fast, lightweight graph database that supports SPARQL, RDF, OWL, and user-defined rules.
:compat-mode!:
:last-update-label!:
:doctype: article 
:docinfo:
:idprefix!:
//:imagesdir: . 
:linkattrs:
:base-url: http://docs.stardog.com/
:cache-uri:
:sectlinks:

image:a.png[] Added new feature &nbsp; &nbsp; image:m.png[] Modified system &nbsp; &nbsp; image:f.png[] Fixed bug

== 3.1 Release
[none]
*ADD Named Graph Security
*ADD Virtual Graphs via R2RML
*ADD Cluster Management tools via Bosh/PCF

== 3.0.2 Release
[none]
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Do not force an slf4j implementation in the Maven dependencies
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improve SPARQL Service Description creation for named graphs
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improve writing index metadata to disk
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Expose configuration for specifying join/write timeouts in the cluster
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Faster inlining of `xsd:double`-typed literals
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better cardinality estimations for property paths
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improve plan caching
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduce lock contention in search indexing to improve its performance 
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced memory overhead of grouping operators `distinct`, `minus`, `group` and some joins.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Faster update of reasoner state when schema does not change
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Supply the missing switch to enable/disable `query.all.graphs` in web console
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improve performance of the processing of user-defined datatypes
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better use of concurrency when computing the reasoner state
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Detect untyped classes and properties outside schema graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exception from reasoner when `rdfs:range` is a `rdfs:Literal`
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing results when `owl:sameAs` is explicitly used in queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing results for some types of queries when using equality reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Ground reasoning queries with unknown properties should not update schema
* image:f.png[Fixed,15,15]&nbsp;&nbsp; CLI data add command should send absolute paths
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query status command prints incorrect timeout value
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Named graph info in SPARL Service Description does not match the spec
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SPARQL Service Description should not include dataset by default
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Avoid caching update plans for `insert/remove data` queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `xsd:double` inlining isn't functional
* image:f.png[Fixed,15,15]&nbsp;&nbsp; 'Reasoning' is misspelled in the form for creating a new database in the webconsole
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing query results with property paths and equality reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Setting `reasoning.type=NONE` should disable schema reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error while parsing query that uses `exists`
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NullPointerException with GROUP_CONCAT
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Plans with property paths are not cached properly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Avoid use of default `false` value for `query.all.graphs`
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Optimizer can select sub-optimal scans over the index
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Duration 0 is shown as Oms
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Computing reasoner state for a large number of named graphs is slow
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Comments with leading spaces seem to cause parse errors
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Order operator is never simple sorted

== 3.0.1 Release
[none]
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improve handling of very large schemas
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced memory usage of reasoner
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better error messages for SPARQL update parsing errors
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect query results within a transaction
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Super properties of topObjectProperty are list
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Property hierarchy contains duplicates
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Parameters assigned to update queries are not set in the where clause
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Explanations for schema inferences inadvertantly extracts instance data into memory 
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Long queries in webconsole produce exceptions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Optimization of property paths could yield incorrect results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; User defined classes that produce bnodes are hidden in schema browser
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Avg of an empty group returns the wrong result
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Variables bound by VALUES clause not treated as join vars
* image:f.png[Fixed,15,15]&nbsp;&nbsp; IllegalStateException: This scan isn't supposed to skip 
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SL filter does not support data some values as superclass 
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Inline optimization fails with sameAs reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Webconsole does not support reasoning.approximate option 
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Overriding query.all.graphs in database configuration does not work
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Literal comparator is broken
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SPARQL Update request with no query returns a 200 via http
* image:f.png[Fixed,15,15]&nbsp;&nbsp; DB creation message contains incorrect info 
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Annex dependency causes maven resolution problems
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Accept `&#42;/&#42;` not recognized
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Long literals not wrapped in the webconsole

== 3.0 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Equality Reasoning
* image:a.png[Fixed,15,14]&nbsp;&nbsp; High Availability Cluster
* image:a.png[Fixed,15,14]&nbsp;&nbsp; LDAP Authentication
* image:a.png[Fixed,15,14]&nbsp;&nbsp; DL instance reasoning (in-memory only)
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Removed reasoning types; reasoning is either enabled or disabled.  Database auto-detects profile
* image:a.png[Fixed,15,14]&nbsp;&nbsp; ICV Repair Plans
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SPARQL SERVICE keyword support
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Maven support
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Custom SPARQL aggregates
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Extension point for customizing the lucene analyzer used during waldo indexing
* image:a.png[Fixed,15,14]&nbsp;&nbsp; User-defined datatype support
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Utility to print information about current license
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Add `--remove-all` option to data add
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New CLI command to undo arbitrary inferences
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New CLI command to fix ICV violations
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New command to import namespaces from an RDF file
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Command to export reasoning schema
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SPARQL built-in support for IF-THEN rules
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Open-sourced Empire bindings (see https://github.com/mhgrove/Empire)
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Deprecated Starman
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Option to abort the bulk load if there is an issue with bulk loading one or more files
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Progress monitor for additions and removals
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Cache strategy for static resources in web console
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Significantly improved write performance
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Significant improvements to query planning
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better handling of large plans during query planning
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Allow SPARQL update queries with VCS commit command
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Search API and CLI support named graphs
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Make query.all.graphs a db option
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Change default CLI construct output format to turtle
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Include rdf:type in reasoning results for variable properties
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Update queries can join existing transactions
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Change the default value of `reasoning.schema.graphs` to all graphs
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Update statistics synchronously for large transactions
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Automatically assume `--server-side` in CLI for localhost
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Option `reasoning.schema.graphs` no longer writable while online
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved procedure for finding license in $STARDOG_HOME
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Make connection timeout a database option
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Change schema tree to load schema info from all graphs
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Use the same name for named graphs argument in CLI commands
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error messages when adding files with unrecognized extension
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Less aggressive plan cache invalidation
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Support Sesame's infer flag
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Show more info for db status command
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Accept 'bz2' as an alias for bzip compression
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved regex performance
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduce lock contention in database when opening connections
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Shiro 1.2.1
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to data-exporter 1.0.4
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Netty 4.0.25
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Removed JLine dependency
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Editing db metadata with webconsole fails
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Need non-zero exit status in the CLI for invalid queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Inconsistency queries with datatypes
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Describe queries with reasoning don't bring type info
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Pellet should ignore ill-formed OWL axioms
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Datatype restrictions with xsd:pattern should be parsed
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database dumps in Turtle don't include prefixes
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Uncaught exception storing large schemas for autocomplete
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ICV convert produces the wrong query for range constraints
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Timed out connections do not specify an error message
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Long transactions should not be automatically closed
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Restored backups maintain path from previous copy
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Commits on differential index can fail on windows
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrupt free page info should not prevent repair
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Namespaces interleaved with RDF yields a `ConcurrentModificationException` when iterated over
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Web Console freeze when jumping from Admin Console to DB Console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exporting a db leaves connections open
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Recursive axioms with literals are not properly unfolded
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exact text search producing unexpected results in web console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Flipping databases offline/online immediately in the webconsole fails
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Online attribute in the webconsole is out of sync
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Datasets do not work with update queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Repair on an in-mem database should refuse to run
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Failing DELETE/INSERT with WHERE update query with search index enabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Clear all against an empty database returns an error
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error parsing object lists of boolean values in turtle
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Graph constants in SPARQL update not handled correctly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SPARQL update parser does not use database configuration options
* image:f.png[Fixed,15,15]&nbsp;&nbsp; INSERT DATA fails when same triple is added to multiple graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect handling of graph keyword in the presence of a dataset
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `StardogDatasetGraph.getNamedModel` returns wrong result
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `StardogDatasetGraph.size` should return the number of named graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Calling `StardogDatasetGraph.end` after commit/rollback raises an exception
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Some configuration options use default serialization making backward compatibility difficult
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SPARQL update query should check write permissions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Queries with MINUS and sp:directType return wrong answers
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Adding empty files hang
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database with durable transactions fail when adding/removing files which do not exist
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Describe queries throw an exception when from graph does not exist
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Editing entity in webconsole strips explicit datatype for xsd:string
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Punning returns incorrect results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Option reasoning.punning.enabled should be mutable
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Guice injector creation deadlocks during init of SNARL client
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ASCII delete char (127) in literals breaks UTF encoding
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reading from dictionary value file should not go past file length
* image:f.png[Fixed,15,15]&nbsp;&nbsp; avg and gmean aggregates can yield a divide by zero error for empty groups
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `server start` dies silently with a non-existent $STARDOG_HOME folder
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Restore breaks on databases with more than 1M quads
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Server side option causes data remove to fail
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Several built-in vocabulary terms are being reported as datatype/object property
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Repair a database missing index.metadata
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database config options that use stardog.properties for the default value are not visible to `metadata get`
* image:f.png[Fixed,15,15]&nbsp;&nbsp; HTTPConnection.admin uses the wrong server URL

== 2.2.4 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; File descriptor leak in cluster proxy
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reasoning write events are not correctly handled by data replication
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ZK and proxy use same java args as Stardog server
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected rolling timeout handling in data replication to better allow for expensive/large replications

== 2.2.3 Release
[none]
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Querying an uncommitted state with reasoning updates reasoner state eagerly
* image:m.png[Fixed,15,16]&nbsp;&nbsp; More optimized implementation for MINUS when using sorted operands
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improve SPARQL query pre-processing in Query Panel
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Added timeout to classification which will revert to RDFS when passed
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Finer grained cancel checks in reasoning operators
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Sesame 2.7.14
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of count aggregate
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Added a default setting for the direct memory in the stardog-admin script
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Transitive symmetric properties in Stardog are also considered to be reflexive
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Validation checks in oracle mode incorrect in tx containing local changes
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Size in transaction reported incorrectly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Redundant atom optimization can incorrectly get rid of atoms containing literals
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cannot clausify some queries using bnodes
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exporting results ignore reasoning parameter
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Queries with reasoning might blow up if typing is ambiguous
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query Panel doesn't recognize db namespaces
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Property chain pre-processing can cause reasoning not to terminate
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Using a graph variable in a query as a join variable will lead to incorrect results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SWRL rules with nonatomic heads might lead to incomplete results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Property paths not using wildcard active graph
* image:f.png[Fixed,15,15]&nbsp;&nbsp; The 'CPU Load' and 'Heap Used' charts might be displayed incorrectly depending on the window size
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Rule extraction does not take int account the schema graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; count(distinct *) does not count the distincts correctly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; "@" and "." are not considered to be legal user name characters
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Failed transaction prevents future transactions when versioning is enabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reasoning with transitive reflexive properties with SL causes class cast exception

== 2.2.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support xpath:subtract-dateTimes
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of BIND operator
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of count aggregate
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better join cost model
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Configuration option for auto stats updates should be writable while online
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Pre-compile all HTML templates for Web Console
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Remove function in Jena interface is not optimal
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of scans joined by a context variable
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced join operator initialization time
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved selection of scan order
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Netty 4.0.23
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of property path evaluation
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cycles in free pages list
* image:f.png[Fixed,15,15]&nbsp;&nbsp; BNodes can't be browsed in Query Panel
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database list in web console displays all databases regardless of user permissions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query Panel results browsing does not include non-default graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Server side export creates empty file
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Running `server status` with only one database results in parse error sending status to client
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Returning duplicate values from a join
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected help content for VCS functions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Display of `index.named.graphs` in web console is backwards
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Order of updates in VCS commit command backwards
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of duration constants with undefined fields
* image:f.png[Fixed,15,15]&nbsp;&nbsp; OOB that manifests itself as a protocol error
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cannot override limit of search results in Waldo
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Jena graph returns incorrect size
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Empty node optimization can cause aggregates to be incorrect
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Named models in Jena to do comply with Jena spec
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Version diff causes NPE
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect projection vars returned in result set for queries with sub queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Enable logging for plans causes incorrect results for queries with filters
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Join optimization can create invalid joins
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Data cycles can lead to duplicate results in property path evaluation
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Filter selectivity estimation incorrect for comparisons of literals
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect results with property paths containing multiple inverses
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Infinite loop rewriting of transitive properties
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Release packaging missing correct vcs HTTP bindings

== 2.2.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Query panel in Web Console supports SPARQL Update queries
* image:a.png[Fixed,15,14]&nbsp;&nbsp; `server status` command to admin CLI
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Bulk load from local files
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Save query button in Web Console query panel
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Method to ExpressionFactory to programmatically create a Stardog Rule from a String
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Optimizations for property path evaluation
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Moved system monitoring into separate page in the web console
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Check $STARDOG_HOME for file system permissions issues before startup
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved help for `role grant`
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Return proper status code for invalid export query format
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved round-trip performance of data-time objects in value canonicalization
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better cardinality of estimation of chains of joins
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Extended coverage of database repair utility
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better cardinality estimations for scans over union named graphs
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error logging on server side
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Throw an error when modifying the reasoning level of a query within a transaction
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Removed concurrency bottleneck in disk lookups of cardinality statistics
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error messages on startup when required jar files are missing
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Homepage content depending on Watchdog is incorrect for community builds
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Queries using "count(distinct *) as ?c" always return a count of 1
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Explanations of update queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing semicolon in .bat files causes classpath errors
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Lost aggregate function when no explicit group provided in query
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect canonicalization of IN filter expressions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `StardogRepositoryConnection.setAutoCommit` starts a transaction
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reference to obsolete JSON-LD library
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query results export throws a parse error
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Literal values in web console should be escaped when inserted into a page
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Inconsistent `getDBOptions` in HTTP Admin falsely reporting `db doesn't exist` message
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Failed update queries print wrong message
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of very large literals (> 8MB) causes problems during bulk loads
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database can be left in locked state under high numbers of concurrent commits
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Direct memory size used for off-heap allocations is incorrect
* image:f.png[Fixed,15,15]&nbsp;&nbsp; JSON Serialization errors for Metadata payloads
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Invalid connection string yields an NPE
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Differential index metadata not flushed to disk
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Explaining IC violations can produce NoSuchElementExceptions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Describe queries are slow with union named graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; False positive detecting 'ask' queries in Query Panel
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Language tags are not rendering correctly in Web Console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Invalid rules are included in the rule base
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reasoner can remain locked if there are errors computing new state during commit
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query Panel route double decodes input parameters
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Overflow error when querying uncommitted state when changes contain duplicates
* image:f.png[Fixed,15,15]&nbsp;&nbsp; In the Web Console, formats in the 'Export as...' drop-down menu on the query panel should be filtered
according to the query that was executed

== 2.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for RDF versioning
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Admin support for webconsole
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Password file similar to postgres for the CLI
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support compressed database exports
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Support acceptable hostnames via CORS
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved recognition of data properties during schema extraction
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Sesame 2.7.12
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Increase default password max length to 1024 characters
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Dropped support for SD-Connection-String header
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Dropped support for SD-Query-Bindings header
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of distinct operator for unsorted results
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Lucene 4.8.1
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Guava 17.0
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved filter selectivity estimations
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error message when searching over non-searchable database
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error message when reasoning consistency check CLI command is incorrectly formatted
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Disabled non-strict parsing when using durable transactions
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Strict parsing now a database option
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Check literals on insert is folded into strict parsing
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Significant reduction in memory usage and increased performance for transferring RDF over the wire
* image:m.png[Fixed,15,16]&nbsp;&nbsp; StardogDatasetGraph implements the Jena Transactional interface
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Deprecate usage of Jena BulkUpdateHandler
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Do not wrap StardogDatasetGraph in DatasetGraphMap, use internal cache instead
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduce default Lucene write buffer size from 1G to 256M
* image:f.png[Fixed,15,15]&nbsp;&nbsp; 'value already present' error when querying using reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Expressions in a projection which are used in an order by are incorrectly parsed
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Direct type does not return results when there are equivalent classes
* image:f.png[Fixed,15,15]&nbsp;&nbsp; "No authenticated user" error when iterating over IC violations
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Constraints using max cardinality does not produce expected violations
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exception thrown due to invalid restrictions during schema extraction
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Web console is not working for IE 8/9
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Interactions between join optimizers can produce suboptimal joins for patterns with unbound predicates
* image:f.png[Fixed,15,15]&nbsp;&nbsp; INSERT DATA with language-tagged literal results in exception
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Inserting a triple/quad with a BNode ID that consists of a single character (e.g., :b)
duplicates this character (:bb)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database exports don't use stored namespaces
* image:f.png[Fixed,15,15]&nbsp;&nbsp; "NQUADS" is not recognized as a valid RDF format
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Transitive properties in PROV are not recognized
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Schema queries do not return distinct results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; "NTriples" is not recognized as a valid RDF format
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Counter of allocated off-heap memory does not take into account allocations via ByteBuffer.allocateDirect
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Filter exists expressions in an optional join cannot be translated into a plan
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Rules containing NOT IN expressions in FILTERs might produce incomplete results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE in schema operator
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ICV guard mode used incorrect reasoning type
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Describe queries return no results when query.all.graphs is enabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query plan explanations when obtained via SNARL protocol do not reflect reasoning level
* image:f.png[Fixed,15,15]&nbsp;&nbsp; CSV and TSV are not recognized as valid HTTP result formats
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Aggregate functions are incorrectly canonicalized
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Temp files created during data uploads for stream storage are not deleted when transaction is complete
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Getting namespaces for db over HTTP returns duplicated mappings
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Batch file to run Stardog fails on Windows when path has spaces in it
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Running search queries without a search index yields an NPE
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Failure of termination condition in an outer join which resulted in an infinite join
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cannot migrate large databases
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Large files cannot be added via HTTP protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Service loader does not ignore comments
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Parse error for xsd:gyear typed literals with the value `1`
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Persistant databases show "Index Persist: No" in webconsole
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect status code for query not found in HTTP API
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE during data obfuscation over SNARL protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cannot export large databases to client
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Losing results with order by & limit from incorrect solution modifier optimization
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Starting server without web console only half works
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Can return incorrect results from index scan when an active graph is explicitly specified
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `ArrayIndexOutOfBoundsException` can occur when multiple projections appear in a plan
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Find method of StardogDatasetGraph is incorrect
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Implement all update functions in StardogDatasetGraph
* image:f.png[Fixed,15,15]&nbsp;&nbsp; A implies {o} is a valid EL axiom, but not accepted by SL profile
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Keys are not recognized as valid EL axioms

== 2.1.3 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for configuration of password requirements
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Override of default URL server does not work
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect error message when failing to connect over HTTP to server w/ HTTP disabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; When custom bind address is used, server start output does not correctly reflect address server is listening on
* image:f.png[Fixed,15,15]&nbsp;&nbsp; BNodes declared in head of insert/delete queries are static
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Need more graceful handling of query bindings header when it's the empty string
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Doubled-typed literal values which are .equals, could canonicalize to different identifiers
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incomplete reasoning with property chains
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Non-deterministic connection failures over SNARL protocol in a server under high load
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SPARQL count optimization can rewrite count expressions to things which are not semantically equivalent
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Commits which contain the removal of an empty named graph yield an NPE during commit to search index
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Update queries via Sesame bindings throw exceptions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Setting query properties on a Sesame query, such as includedInferred, does not work as expected
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `Repository.getContextIDs` returns a non-unique list of contexts
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Getting the size of a specific named graph via `StardogRepositoryConnection` yields an `UnsupportedOperationException`
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Shells scripts do not quote paths which fails in the presence of paths with spaces and the like
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Memory mapped tx log files on Windows can get mangled when mem-mapped contents are flushed to disk
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Computation of very large query result sets slows down due to cache thrashing
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Starting server with `--no-web-console` should print a message
* image:m.png[Fixed,15,16]&nbsp;&nbsp; CLI export help needs some clarification

== 2.1.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Auto-complete feature in Web console's query panel
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Load query from file in Web console's query panel
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SNARL API support for performing an export
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Server side export support
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Arithmetic operations for date/time values
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrectly handling filter inlining when variable in OPTIONAL side of join
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Most recent version of chrome breaks the webconsole layout
* image:f.png[Fixed,15,15]&nbsp;&nbsp; CORS method `OPTION` is missing
* image:f.png[Fixed,15,15]&nbsp;&nbsp; DISTINCT operator could return only a REDUCED result set in some cases with reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Correctly verify property paths during optimization when both start & end point are unbound
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Rewritten plans might incorrectly share variables which can lead to empty results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; In the Web console, prefixes included in the query panel seem to be irrelevant
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected rendering of accented characters in welcome screen
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Export cli function does not use RDF format aliases
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Unexpected result for HAVING (bound(...)) expression
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect handling of function expressions within a bind
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Do not create superfluous temp files which are not used or deleted
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Be less aggressive about moving intermediate results off heap
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Failure of termination condition in an outer join which resulted in an infinite join
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error message using using non-existent file to query command on CLI
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Sesame 2.7.10
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Protobuf 2.5.0
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Netty 4.0.17
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Lucene 4.7.0

== 2.1.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Several new SPARQL filter functions from the .NetRDF library
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Allow RDF strings to be passed as input to explain command
* image:a.png[Fixed,15,14]&nbsp;&nbsp; CLI explain option to return multiple explanations
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Explanation merging to handle more cases 
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Obfuscation CLI commands to work with config files
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Obfuscation CLI commands to handle SPARQL rules properly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Regex filter persisted over multiple queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cast exception during describe query evaluation over named graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Properties which are both symmetric and transitive are not handled correctly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect mime-types sent to server for file references
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Some optionals, when mixed with distinct, still do not return distinct results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Large numbers of unique BNodes in nq, trig, and trix formats cause RIO to use too much memory
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Buffers used during bulk loading can take up too much memory when large literals are used
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Fallback handling of access to sun.misc.Unsafe for JDK's which do not support it
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved filter pushing optimization
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Warning printed from 'role grant' command when multiple actions are provided

== 2.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for bzip2 compressed input files (with extension .bz2)
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Auto-repair mechanism for common types of index errors
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Generation of multiple proofs for inferences & inconsistencies
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Database repair, backup & restore utilities
* image:a.png[Fixed,15,14]&nbsp;&nbsp; FROM/FROM NAMED 'default' can be used as an alias to query default graph
* image:a.png[Fixed,15,14]&nbsp;&nbsp; 'dbms-admin:shutdown' permission to allow non-superusers which hold that permission, to shutdown the server
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Improved query scalability by flowing intermediate results off-heap or onto disk.  Note: flowing off-heap requires an Oracle JDK due to its use of sun.misc.Unsafe
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Significant improvement in performance of bulk loading and total scalability of a database
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Named Graph information in proofs
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Proofs for integrity constraint violations
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NaN displayed in waldo search results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Web console hangs when using with SSL
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Log redirection at startup messes up the logger output
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Over-eager update plan caching resulting in different update queries executing the same update
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Search index is not needlessly rebuilt on stardog
* image:f.png[Fixed,15,15]&nbsp;&nbsp; HTTP server on OpenJDK does not always close connections
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Web console not loading on Win7 when Stardog is run as a service
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Stardog Rules do not use the pre-defined database namespaces
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Encoding of special characters in web console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue in query optimizer where a more optimal plan could accidentally get discarded
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Built-in values (such as owl:Thing) used inside filters are not correctly recognized
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Queries using reasoning and FILTER NOT EXISTS could return incomplete results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cannot shutdown server over HTTP if security is disabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Functions with zero args, such as BNODE(), can produce incorrect results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; now() returns the time the server started, not the actual current time
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Asking for the types of a class yields an exception
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Axioms using HasValue expressions can case type queries to produce incorrect results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Value for subject in THEN clause is not returned
* image:f.png[Fixed,15,15]&nbsp;&nbsp; BIND optimizer can incorrectly order the BIND expressions
* image:f.png[Fixed,15,15]&nbsp;&nbsp; IF-THEN rules might fail to recognize term types
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Windows service does not always shut down cleanly
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced memory footprint of queries while being executed
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Optimizing a database will also rebuild its search index
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Make database namespaces meta property creatable
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Database meta properties cannot be re-initialized to prevent malicious users from changing their behavior
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Netty 4.0.14
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Sesame 2.7.7

== 2.0.3 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; manpages
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for punning when using reasoning & rdf:type
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Browse tab was missing classes in IE9
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in Watchdog API JSON serialization
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Watchdog information in web console not visible on Windows
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in rendering of columns in web console homepage in IE & FF
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing elements in web console in IE
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Correctly handle line breaks in proof tree serialization
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing final byte when formatting query results using 'Accept-Encoding: gzip,deflate'
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Parsing of queries which use the Stardog directType built-in
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance of web console in IE
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved group help, including full synopsis, in CLI

== 2.0.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for the "Accept-Encoding: gzip" header over HTTP; enables sending of gzipped responses.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Graph store protocol to support gzipped content
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved load progress monitor based on the number of bytes read
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Deprecated the SD-Connection-String header, now specified as a parameter to the request
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Guava 15
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Correctly handling specifying the dataset over SPARQL protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in literal canonicalization
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bogus warnings about absorption from Pellet
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Re-indexing of searchable databases regardless of whether or not the search index needs to be updated
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Pressing key shortcut multiple times for the help in the web console displayed the help multiple times
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Construct queries do not produce results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Not able to add properties to individuals in web console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query over partitioned graphs in Jena does not work
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Using :textMatch in a query against a non-searchable database causes the web console to spin forever
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Typing a number in query panel results paper gives an error
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Blank lines pre-pended to SPARQL queries in web console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Negative execution times for queries when retrieved over SNARL protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; HTTP server accepting gzip files for data add/remove commands
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Strict parsing turns off RDF/XML parser checks some of which are very memory intensive
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Disabled BNode caching in N-Triples parser which are very memory intensive
* image:f.png[Fixed,15,15]&nbsp;&nbsp; 400 errors when db name is included in connection string header
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Loading of database options in CLI so they can correctly be set
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Evaluation of queries using distinct with a complex, expression-based sort in the order by clause
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Correctly send challenge response in all cases to avoid a browser caching bad credentials
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in proof rendering over HTTP
* image:f.png[Fixed,15,15]&nbsp;&nbsp; SWRL rules containing non-atomic body atoms could cause incorrect results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; 'db status' admin CLI command returned an incorrect size for the database
* image:f.png[Fixed,15,15]&nbsp;&nbsp; HTTP server OOM when a large Content-Length for an incoming POST is used
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Transactions state on the client was not correctly cleared in some error cases
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of large gzipped files in the HTTP protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Translation of rules with star-shaped body atoms

== 2.0.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for GZip encoded files in the HTTP protocol
* image:a.png[Fixed,15,14]&nbsp;&nbsp; More extensive reasoning functionality exposed via ReasoningConnection
* image:f.png[Fixed,15,15]&nbsp;&nbsp; System DB corruption due to starting two Stardog servers using same home directory
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exporting data in web console occasionally hangs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; 5822 is no longer the default port for HTTP protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Context indexes sometimes out of date after a named graph update
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `--disable-security` flag actually disables security complete, works for both HTTP & SNARL protocols
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error when using user supplied prefixes in a query via the web console
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Multiple key shortcut dialogs can be displayed at once
* image:f.png[Fixed,15,15]&nbsp;&nbsp; IO error with large responses over HTTP
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Web console loading issues in Windows 7
* image:f.png[Fixed,15,15]&nbsp;&nbsp; DB corruption when multiple Stardog servers are started at once
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Improved handling of completely unbound patterns during reasoning avoiding incomplete results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Better error response from server when authentication fails
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved concurrency issue in ICV guard mode validation when competing threads are attempting to validate & commit
* image:f.png[Fixed,15,15]&nbsp;&nbsp; `.bat` files can be run from outside of the bin directory and better respect setting STARDOG_HOME environment variable
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Unified SPARQL & SWRL UUID functions
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better handling of duplicate results in construct queries including when projection uses fully-qualified results
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved feedback for adding/removing data via the web console
* image:m.png[Fixed,15,16]&nbsp;&nbsp; External links opened in new tab in web console
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced memory overhead of streaming modifications
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved bulk load performance for files without named graphs (RDF/XML, Turtle, etc.) 

== 2.0 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SPARQL Update
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Web Console
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Proofs for reasoning explanations
* image:a.png[Fixed,15,14]&nbsp;&nbsp; JMX monitoring
* image:a.png[Fixed,15,14]&nbsp;&nbsp; PROV support
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SL reasoning profile
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Client & Server dependencies are cleanly separated
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Evaluation of non-recursive datalog queries to improve reasoning performance
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SPARQL-based IF/THEN rules syntax
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New, significantly faster, SPARQL parser
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Native SSL support for SNARL & HTTP protocols
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Single Stardog server, both protocols run on the same port within the same server.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; CLI commands to obfuscate RDF data and SPARQL queries for sharing sensitive data
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Type detection of variables in queries with reasoning
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Ability to load server-side files via the SNARL API
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for Sesame directSubClassOf, directSubPropertyOf
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Filter expression inlining when used with a group-by expression
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Lock upgrading in search connections which resulted in deadlock in some circumstances
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ICV export added to shell completion scripts
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error in reasoning where some rules could prevented rewriting
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of '=' in namespaces
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exception thrown by describe queries via CLI
* image:f.png[Fixed,15,15]&nbsp;&nbsp; EOF exception over HTTP for large datasets
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Rewriting error when punning is used
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Leading comment characters break query type sniffing
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Auto closing of result sets when using embedded connections
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error in named graph lookups in Jena bindings
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in reasoning where equivalent classes/properties were not correctly accounted for
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Querying for properties/individuals via OWL's built-in classes produced empty results with reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in reasoning leading to incomplete answers when punning is used
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corner case in sameterm inlining where the expression was incorrectly bound
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query answering where the variable used in a group by was not bound
* image:f.png[Fixed,15,15]&nbsp;&nbsp; The command 'server start' now supports --logfile option on Windows too
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of limit & offset in searches on the CLI
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Parse error for small threshold values in search queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE during the evaluation of some FILTER EXISTS queries where the predicate is not bound
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Filter comparison for literals of unequal datatypes when the literals are inlined into the query
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Printing search results in a formatted table
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Using a single namespace for Stardog extensions & default URIs
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better error messages for malformed literals which create value errors during query evaluation
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Removed unused -u,-p,-P flags from server start
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Search results on CLI sorted by score
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Minimized roundtrips to security module to reduce overhead
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved literal comparisons in filters
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved query planner performance
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Significant improvement to write performance
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated Netty to 4.0.9
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated Sesame to 2.7.3
* image:m.png[Fixed,15,16]&nbsp;&nbsp; SNARL API, Jena & Sesame bindings

== 1.2.3 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; ICV export feature to CLI
* image:a.png[Fixed,15,14]&nbsp;&nbsp; fish shell autocomplete scripts
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SPARQL constraints can be added directly via CLI
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing completions from bash autocomplete scripts
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Improved error messages when using the CLI
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Using variables in the predicate position with reasoning lead to
incomplete results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Deserialization errors when commas are used in namespace declarations,
for example in tag URIs.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Add/Remove data over HTTP with strict-parsing enabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of strict-parsing when creating a database, option was
ignored
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved help content for default options in CLI
* image:m.png[Fixed,15,16]&nbsp;&nbsp; ICV CLI commands to use admin credentials by default

== 1.2.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; ln, log, atan, asin, acos, sinh, cosh, tang, toDegrees & toRadians to
Stardog built-in function library.  These are in the tag:stardog:api:functions: 
namespace.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE when using SNARL protocol with a select query that has no variables.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Removed a source of non-determinism during query rewriting leading to less
variance in performance of queries with reasoning
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected namespace rendering in explanations
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Exception handling in begin/commit/rollback in Empire bindings
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Improved performance of rewriting in queries where atoms are repeated
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in iteration cursor propagation during a join
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected execution order planning of chains of dependent SWRL builtins.

== 1.2.1 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Order of parameters for CLI commands data add and data remove
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Autocomplete for 'stardog query' is fixed
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Added hooks for SWRL trigonometric functions (sin, cos, tan) in
the query engine.  These functions are available in the SWRLB namespace:
http://www.w3.org/2003/11/swrlb#
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Modified the service loader to resolve issues that arise in
multiple classloader environments, such as in a servlet container.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved issue where SWRL rules with bound arguments in the head
of the rule produced incorrect results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected the execution planning for SWRL rules where multiple
built-ins are chained together.

== 1.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Query management functionality to list and kill running queries
(API, SNARL, HTTP, CLI extensions).
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New configuration option to automatically kill queries when
execution time exceeds a timeout (default 5 min).
* image:a.png[Fixed,15,14]&nbsp;&nbsp; A new logger to log information about slow queries.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Extend Connection interface to easily login to StardogDBMS to
perform administrative action.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New CLI with improved online help, command groups, and consistent
response codes.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SNARL and HTTP protocols support queries that use stored namespaces
similar to CLI.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New transaction layer which uses async writes & mem mapped logs to
improve durable logging performance.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for the ARQ builtins pi, e, sqrt, min & max.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; HTTP server now supports the SPARQL Service Description spec.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New, more performant security layer.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Explain data inconsistencies via the reasoning connection.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Better caching of rewrites of query plans to improve reasoning
throughput
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Improved performance of write caching as requests to add/remove
data are appended to a transaction
* image:f.png[Fixed,15,15]&nbsp;&nbsp; CLI error messages are not consistent regardless of which protocol
is used to connect to the database.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of several SWRL builtins, including comparison operators
such as greaterThan or lessThanOrEqual, such that their use yields correct
query results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved an issue where lookup queries from Stardog's
SesameRepository implementation where incorrectly handled by the plan cache.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Applied better ordering to conjuncts in a rewritten plan to improve
determinism in reasoning query performance.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Jena named Datasets now work correctly
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Calling ConstraintViolation.getViolations a second time in HTTP
connections returns empty results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Printing very long literals without whitespace in query results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in plan caching where unknown constants introduced in reasoning
could result in incorrect query results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Closed vulnerability to billion laughs attack.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query parsing bug where the name of variable could affect the
resulting algebra.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ICV security permissions...now correctly set permissions for
specific access to ICV constraints independent of DB access.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Using an unsupported output format while displaying the user/role
permission table no longer fails silently.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Property paths to detect cycles now correctly return results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Lock issue with concurrent writes to the Waldo index
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Moved search related configuration options from DatabaseOptions to SearchOptions.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Output of query CLI function is less verbose, but supports
`--verbose` to get additional information.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Whitespace is no longer allowed in passwords, and passwords must
be at least four characters long.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Database, user, and role names now have a maximum size of 64
characters.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Password must be specified when creating a new user
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Additional step of describing a literal during search
indexing is now optional, and disabled by default which increases search
indexing performance in most cases.  Can be enabled by setting the
'waldo.describe.during.indexing' option in your stardog.properties file.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated to Sesame 2.7.0, Jena 2.10.0, Lucene 4.2.0, Shiro 1.2.1.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Online & Offline no longer take a wait/no wait option, they
are synchronous actions. The current methods are left in the DBMS API,
but deprecated, to be removed in the next major version. If you are using
these methods from the CLI and need them to be asynchronous, background the
task. If you're using them programmatically, use a Thread.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; 400 code is returned from the server if an invalid name is
provided for a database.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; More comprehensive Stardog-specific error codes returned from
the server in the HTTP protocol in the response header.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Removed the Stardog Shell from the distribution.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Removed `http.max.connections.per.route` configuration option
* image:m.png[Fixed,15,16]&nbsp;&nbsp; `autoCommit` and `remove(Query)` are deprecated from the Connection API, to be removed in the next release.

== 1.1.5 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query optimizer where the sort order was not correctly taken
into account resulting in poor plan selection.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of BGPs where a Literal value is used as the subject.
Avoiding inadvertently assuming it's a URI and returning results when
performing reasoning, or throwing a cast error.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved issue with memory usage of the admin client
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New option 'leading.wildcard.search.enabled' which can be
configured in stardog.properties, for enabling the leading wildcard search
support in the Lucene QueryParser.

== 1.1.4 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; CLI commands that output tabular data might trim results (depending
on word wrap boundaries).  This resolves namespace metadata updates not
being visible.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Server stop now correctly prompts for password when -P is used.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed the error in query rewriting that lead to the cryptic
"Invalid term in query" error on a client.

== 1.1.3 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; A new configuration option to control preserving bnode ID behavior
in parsing.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; More efficient copy of http multi-part attachments when creating
a database via the http admin interface.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Using ThreadSafeClientConnManager in Admin HTTP client.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; ICV translation from OWL to SPARQL now uses SPARQL 1.1 (FILTER
NOT EXISTS)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Using same internal ID for bnodes coming from different files.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Unsupported exception throw for symmetric properties in reasoning
queries.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug with reasoning queries containing OPTIONALs.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reasoning is not used for the expressions inside FILTER [NOT]
EXISTS.

== 1.1.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New optimizer for sub queries.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Optimization of EXISTS queries to better account for runtime
usage.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in cached query solution handling in joins.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved two issues with the evaluation of nested property paths.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Missing query results while updates on other threads are continuing
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Empty passwords no longer allowed.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Properly displaying user's permissions when same set of actions was
granted by different roles.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Displaying all roles that granted user a permission.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Displaying user's permissions in the case when the executing user
has read permission to the user resource but not some of its roles.

== 1.1.1 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Deadlock in Connection.remove().query() when in embedded mode
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Duplicates in search results caused by repeated addition of a
triple to the database
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Non-termination in evaluation of largely nested filters
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Non-termination in evaluation of nested optionals within graph
patterns
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Corrected issue in method which finds transactions in logs
requiring recovery
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error in HTTP client where last byte of payload could be truncated
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue in error code translation in the HTTP client
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Connections to HTTP server were not getting closed when
invalid/supported mime-types were requested for query responses.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved issue where filter exists was not using the active graph
as specified in the from/from where clause of the query.

== 1.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for transitivity reasoning.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SWRL support.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; SPARQL 1.1 Query support.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; directType, strictSubclassOf & strictSubPropertyOf support.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Limited support for creating new individuals in your rules.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Grouping quads by their named graphs in the export output for
prettier results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Top & Bottom properties are not incorrectly included in results of
queries with reasoning.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NullPointerException when using sameTerm filter with a variable
that was unbound due to an optional or union.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NullPointerException in SNARL client connection when client is used
in concurrent environment.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect results when reasoning with reflexive properties in some
corner cases.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Clear & Remove context where not getting correctly propagated to
transaction logs.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Clear all was not clearing search index.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; sameTerm inlining optimization was violating scope of a graph
clause.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Waldo full text searches over contexts (using from/from named or
graph) were not always correctly setting the context of the results.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Assertion of contexts in the search index is correct when bulk
loading.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Could not assign r/w permissions directly to database metadata.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved bug in security where only write access was required for
reads once a transaction was active on a connection.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query planning that could cause it to not terminate for
queries with deeply nested optionals.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Old versions of statistics (on disk) are correctly cleaned up on
Windows systems.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Can now revoke all permissions a role/user has for a resource.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Faster triple pattern evaluation with reasoning for patterns
using rdf:type when other patterns bind the subject or object.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; New search indexing algorithm which computes search indexes in a
single pass over the RDF index for a significant speed improvement
* image:m.png[Fixed,15,16]&nbsp;&nbsp; New format for search indexes
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Sesame API bindings.  StardogSail has been deprecated & removed.
Users should stick with StardogRepository.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated Jena to 2.7.4
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated Sesame to 2.6.9
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated Lucene to 4.0.0
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated OpenRdf Utils to 0.8 & CP Utils to 2.3

== 1.0.8 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Accept Contexts.ALL as a synonym for "clear all" in the HTTP
endpoint.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue with queries involving reasoning that used Thing or Nothing
in filters, which in some cases, could return incorrect answers.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Granting a permission to a role no longer causes the Role to show
up in the Users list.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Dropping a Role which is still in use now throws the correct type
of exception
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE during evaluation of a query which uses a variable in a filter
that is not used in a BGP.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Clarified documentation in admin create (database) regarding
whether or not name is required
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Updated javadocs for Connection to discuss some thread safety
issues and a note in Query regarding injection attacks and correct use of
the parameter method.

== 1.0.7 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Filter pushing optimization which will move filters deeper into
plans when it appears to be advantageous to do so
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Literal comparison improved to increase filter performance
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved inlining of constants from filter expressions
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improvements to bulk loading at database creation time resulting
in up to 20% increase in load speed.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved add/remove performance to existing databases
* image:m.png[Fixed,15,16]&nbsp;&nbsp; More aggressive flushing of database contents to disk to
minimize risk of data corruption when Stardog server is not properly shut
down

== 1.0.6 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; File streams from HTTP add/remove payloads are closed and cleaned
up in all cases, reducing the number of open temp files.  Improves shutdown
performance
* image:f.png[Fixed,15,15]&nbsp;&nbsp; text/turtle & application/ld+json now work for the HTTP protocol as
return format mime types
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Error in query evaluation of deeply nested outer joins
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue with a rewriting optimization corner case that could result
in non-termination

== 1.0.5 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Evaluation error in Union skipping & error handling in both SNARL &
HTTP protocols
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect results when unions are used in queries which are
rewritten
* image:m.png[Fixed,15,16]&nbsp;&nbsp; New query planner introduced

== 1.0.4 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Ability to convert Integrity Constraints to SPARQL queries using
the API or the CLI
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for running Stardog as a Windows service
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for parameteric queries in CLI query command with (-b,
--bind) option so variables in a given query can be bound to constant values
before execution
* image:f.png[Fixed,15,15]&nbsp;&nbsp; CLI command "explain plan" works with queries that use namespaces
stored in the database
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Reasoning queries returning incomplete results while retrieving
inverse property inferences
* image:m.png[Fixed,15,16]&nbsp;&nbsp; CONSTRUCT query results in HTTP protocol use namespace prefixes
from the query to return more concise output
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Error checking in audit/access log configuration to report more
informative error messages

== 1.0.3 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issues with batch files in Windows when JAVA_HOME was set to a path
containing a space
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in propagation of errors midstream in the SNARL protocol
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug where sort order information on unions in a plan was lost
resulting in slower queries
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Ability to load files from a directory recursively
* image:a.png[Fixed,15,14]&nbsp;&nbsp; ReasoningConnection.getBaseConnection to provide access to
underlying (nonreasoning) connection
* image:m.png[Fixed,15,16]&nbsp;&nbsp; New log file header record added with log metadata
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Included the log file descriptor, log.proto, in distribution

== 1.0.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Stardog Community version and COMMUNITY_LICENSE.txt
* image:a.png[Fixed,15,14]&nbsp;&nbsp; IC validation support in SNARL and HTTP protocols
* image:a.png[Fixed,15,14]&nbsp;&nbsp; HTTP Admin protocol calls to add databases, users, roles
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for SPARQL 1.1 Graph Store Protocol (with the exception of
PATCH method)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issues with concurrent reads and writes
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug with duplicate results with queries containing UNIONs.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved performance for concurrent connections updating the
database
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Server start message to clarify Stardog listens on all network
interfaces
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Classpath manifests in Stardog jars so only CLI jar defines a
classpath
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Netty jars to version 3.5.2
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Moved LICENSE.txt to EVALUATION_LICENSE.txt

== 1.0.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Stardog documentation is now served by the Stardog HTTP server at
the URL http://host:port/docs
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Store prefix mappings inside Stardog database metadata and prefix
declarations are automatically added to queries executed through CLI and
used in query results
* image:a.png[Fixed,15,14]&nbsp;&nbsp; HTTP protocol for Stardog admin commands that lets any Stardog
admin command (creating a database, adding users, changing permissions,
etc.) can be run through HTTP in addition to the default SNARL protocol
Note: if you contemplate implementing a client of the HTTP API, it is
not yet stable and a few parts of it will change in the next (1.0.2)
release.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Access and audit logging in Stardog server to log events such as
authentication events, queries and transactional change
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Make login functions in StardogDBMS publicly accessible
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Validation problems in CLI for very long query strings
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Commits hanging when durable transactions option is enabled
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Stardog sometimes failing to generate an error for syntax errors
during add even though no data is added

== 1.0 Release
[none]
* image:m.png[Fixed,15,16]&nbsp;&nbsp; New login functions in StardogDBMS to clarify which server
(embedded or remote) we are connecting to
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling DESCRIBE queries where triples about the resource to
describe occur in named graphs
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling DESCRIBE queries with multiple resources to describe
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Handling DESCRIBE queries with multiple resources to

== 1.0 RC1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Batch files for executing the Stardog command line in Windows
* image:a.png[Fixed,15,14]&nbsp;&nbsp; directSubClassOf & directSubPropertyOf (same sparqldl namespace
that is used by Pellet 'http://pellet.owldl.com/ns/sdle#') which can be used
to get the explicit sub class/properties in a query that is using reasoning.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New command line function to check whether or not the database is
consistent.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New command line function 'explain inference' to explain reasoning
results (existing 'explain' command has been renamed to 'explain plan' to
avoid confusion).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved issue where an unbound type operator, "?s a ?o" produced
incorrect inferences for some ontologies where there is interaction b/w
domain/range & hasValue axioms.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in the checking of user & role resource names when they're
using the All permission, eg "user:*"
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Consistency checks will not fail (say the db is inconsistent) when
the consistency check requires reasoning services not provided by Stardog,
such as datatype reasoning.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Deadlock in bulk loading which can occur when the bulk load exceeds
5B triples.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Consistency checks for functional data properties which require
literal value validations
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in axiom filtering for the EL profile that let non-EL axioms
into an EL knowledge base
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in reasoning in the type operator where nominal are used within
an equivalence or subclass axiom
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Race condition in index commits in highly multi-threaded
environments
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Clarified error message when using search but the database does
not have search enabled
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Consistency checking performance improved
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Bulk loading performance improved at larger scales
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Small reduction in memory overhead of query response streams

== 0.9.6 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Checking consistency of a database over HTTP.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved regex issue used to validate input to security where some
valid inputs could be rejected.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Multiline queries passed to CLI explain are now correctly parsed.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Inconsistent usage of base URI used for parsing when one is not
specified by the user.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed an issue with automated TBox extraction where some parts
of the ABox were incorrectly put into the TBox resulting in slower than
expected performance for queries involving reasoning.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in reasoning query evaluation where the property variable is
unbound which resulted in incorrect inferences to be returned.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Password decoding in SNARL was incorrectly padding when converting
to/from UTF-8 resulting in passwords sent over HTTP failing to match the
password in the user database which only occurred with passwords over a
certain length.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Queries with reasoning where the predicate in a BGP is unbound does
not correctly return explicit statements when the schema is not present.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Context variables, such as "graph ?g { ... }" are correctly used in
joins and used for join ordering.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue with decimal inlining where negative scale values were
handled incorrectly resulting in decimal values being mangled on load.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Avoiding reclassification of TBox after commit when the TBox
contents did not change.  Significant improvement in commit performance for
databases with expressive TBoxes.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Handling of add/remove operations on the server resulting in
decreased memory overhead and up to a 50% increase in the time it takes to
process the change.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; DL queries that involve ABox operators now cause errors at query
time.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Inferences involving Top/Bottom Object/Data property are
ignored.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Offlining a database will drop the database completely out of
memory reducing server overhead of having large, offline databases.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Increased packet size in SNARL protocol changing maximum size
from 64k to 16.7M bytes.  This raises the maximum size of a triple or query
that can be exchanged to the protocol to nearly 17MB and should avoid any
issues with loading datasets with large literals.  Earlier versions of
the SNARL protocol from 0.9.5 and earlier are not compatible with the new
format.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Storage format of RDF concepts loaded into Stardog has been
changed.  This is a backwards incompatible change in format and no migration
is possible.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Significant performance increase in loading throughput through
better utilization of system resources to perform concurrent loading and in
a more compact on disk format for some parts of the database contents.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Handling of typed literal values in query evaluation resulting
in performance improvements in queries which perform math & comparison
operations on datatyped literals.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced overhead of query plan generation resulting in
performance increases for smaller queries where execution time does not
dominate planning time.

== 0.9.5 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Name of index copy target is validated so you cannot copy a
database and give it an invalid name.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Printing result of ASK queries in the CLI.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Format parsing for construct queries on the CLI where unrecognized
format types were resulting in NullPointerExceptions.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Databases which fail to initialize are no longer added to the
server.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Better cleanup of databases which failed to be created.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query explain over HTTP POST works and no longer returns a
NOT_ACCEPTABLE response.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Increased performance of database copy.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Clearer error message when the server subcommand is mistyped.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved access to disk indexes resulting in significant query
performance improvements in many workloads.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Clearer output messages from the 'drop' CLI command.

== 0.9.4 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Order by will correctly use secondary, tertiary, etc. orderings.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Null values in sorting are correctly sorted as the lowest value.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved issue related to reusing a variable multiple times in the
same BGP, correct results are now produced.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Changed handling of Channel close events in database server to
avoid endless propagation back to the client, particularly on Windows.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database cleanup on close events to avoid corrupting metadata.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Handling of type errors in regex filter.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; New algorithm for handling ?x rdf:type ?o and :inst rdf:type
?o BGP's in queries with reasoning resulting in significant performance
improvements.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; ChannelFactory is shared amongst all clients in the same JVM to
avoid OOM errors due to an issue with Netty (NETTY-424).
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Login process to obtain a connection to a database changed to
reduce connection overhead.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Shiro session timeout handling to avoid erroneous timeouts
during long processes, such as bulk loads.

== 0.9.3 Release
[none]
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Server start script gracefully handles the situation when
STARDOG_HOME does not exist, is not a directory, or is not writable
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Connection string parameters are now validated, errors are
thrown when unknown parameters are provided.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Return result of empty query results in SPARQL JSON format when
accessing the HTTP query endpoint directly.  Returning an empty binding set
instead of empty JSON.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Checking of Accept headers for the query HTTP endpoint to better
handle the case when multiple Accept headers are sent to the endpoint, but
only one is relevant for the type of results to be returned.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reporting of query execution time of select queries in CLI to
better reflect total execution time.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved issue in query generation arising from a variable being
used in a BGP, context statement, and within an optional causing no results
to be returned.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query planner where a join variable was also used in a
filter with an equality test.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in ICV when datatype restrictions are used in conjunction with
a datatype property
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Dropping database on Windows now works; Windows was holding files
open and we could not delete them.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE when retrieving an unknown option for a database
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Inferences were missed because hasValue restrictions were
incorrectly filtered in RL & EL profiles

== 0.9.2.1 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE when retrieving user permissions

== 0.9.2 Release
[none]
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Security layer implementation changed to provide better
performance when manipulating user, role, and permission information
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reasoning & ICV interfaces to be more consistent along
consistency checking & validation
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Major improvements to query rewriting performance.  Rewrites are
up to 3x faster, resulting in a noticeable improvement in executing queries
with reasoning
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved CLI help
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Friendlier error messages when invalid SPARQL queries are
provided in the command line
* image:m.png[Fixed,15,16]&nbsp;&nbsp; More verbose startup & shutdown logging information
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better output for ICV validation when the database is trivially
valid
* image:m.png[Fixed,15,16]&nbsp;&nbsp; A solution to quickly find nearly optimal query plans in case
plan optimization takes too long
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved hash join algorithm for faster processing in case there
are multiple alternative join keys
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query plan caching that could result in incorrect plans
being used for similar queries
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed cardinality estimations for some named graph scans
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Significantly better cardinality estimations for large plans,
particularly involving multiple unions.  Can result in improvements of up to
50% when bad joins can be avoided.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in ICV when getting constraints for a new database that does
not yet have any constraints
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE that can arise during query evaluation after loading a large
dataset
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Searchable configuration property can now be changed after creation
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Database load errors no longer prevent kernel from starting
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Autocompletion for local files in Stardog Shell works regardless of
other parameters used in the command

== 0.9.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Stardog Shell
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Auto-update of statistics now lockless
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Made visible extension point for custom SPARQL functions.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cardinality estimation for queries across named graphs

== 0.9 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new native protocol, SNARL, based on Netty and Google's Protocol
Buffers
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new system catalog for all system info, metadata, etc.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new search system over RDF literals, integrated with SPARQL query
eval
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new server management: online, offline databases; better server
startup, shutdown, and locking
* image:a.png[Fixed,15,14]&nbsp;&nbsp; CLI now supports remote db administration, many new subcommands
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new Quartz-based job scheduler with cron expression scheduling
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new, optimized index support for named graphs and triples-only
databases
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new, differential write indexes with customizable merge thresholds
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new customizable TBOX (i.e., reasoning schema or ontology)
extraction & management
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new Stardog configuration system, including database creation
templates
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new reasoning services (explanation, satisfiability, consistency
checking) available programmatically
* image:a.png[Fixed,15,14]&nbsp;&nbsp; reasoning extended to named graphs
* image:a.png[Fixed,15,14]&nbsp;&nbsp; ICV validation extended to named graphs
* image:a.png[Fixed,15,14]&nbsp;&nbsp; new or improved support for NQUADS, Trig, TSV, and CSV
* image:a.png[Fixed,15,14]&nbsp;&nbsp; improved concurrency in data loading
* image:a.png[Fixed,15,14]&nbsp;&nbsp; consistent use of <tt>mmap()</tt> for better performance
* image:a.png[Fixed,15,14]&nbsp;&nbsp; load GZIP-compressed files directly, including ZIP files with
multiple RDF
* image:m.png[Fixed,15,16]&nbsp;&nbsp; new security system, including user management, granular roles
and permissions model
* image:m.png[Fixed,15,16]&nbsp;&nbsp; new statistics computation & recomputation algorithms
* image:m.png[Fixed,15,16]&nbsp;&nbsp; updated documentation
* image:f.png[Fixed,15,15]&nbsp;&nbsp; many (many!) known bugs

== 0.7.3 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New SNARL Connection view, ReasoningConnection which exposes the
functions provided by the Stardog reasoner.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support in ReasoningConnection for obtaining the explanation for an
inference.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Explanations of IC violations as the expected and/or missing
statements that make up the violation
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New example for using explanation facilities, updating ICV example.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved error messages when CLI fails to connect to a database.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in the handling of reflexive properties when they involve TOP
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in initialization of TOP operator with bound objects, resolves
NPE in execution
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in bnode handling in TBox extraction

== 0.7.2 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; RDFS & DL reasoning types.  DL reasoning type supports schema-only
queries.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Execution optimizations for when TOP appears in a query due to
reasoning
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Sesame Repository/Sail implementation to use a SNARL Connection
per Sail/RepositoryConnection rather than per Repository.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better formatting of query results in CLI query command
including new formatting options.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Retrieving ranges of data properties in reasoner.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Read transaction isolation bug for mulitple connections against the
same database.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug related to incorrect reassociation of Shiro auth credentials
with pooled connections.

== 0.7.1 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Canonicalization of literal values on input, either bulk loading or
normal add operations.  Canonlicalization can be disabled when creating the
database if it is not desired, but is on by default.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; CLI connection strings no longer assume you are connecting to
an embedded store when no protocol is supplied.  You must now specify a
protocol.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ExpressionFactory is no longer stripped from the core jar file
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Multiline queries work with CLI now
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in loading IC's from file, occasionally constraints could be
malformed

== 0.7 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Bulk insert/removal performance on existing databases greatly
improved via concurrent application of changes to the index and the
introduction of a differential index.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Stardog reasoner now supports OWL2 EL and RL reasoning types.
These can be specified at connection time and EL & RL reasoning is performed
at query time the same as QL.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for 'hybrid' and TBox queries for all reasoning types.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for Integrity Constraints.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Propogation of reasoner state amongst open connection to the
reasoner, state is better shared across connections requiring less overhead
to maintain the up to date state.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; 'delete' was renamed to 'drop' in the command line.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in two-phase commits for changes to a reasoner

== 0.6.10 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed an issue with not always consuming the HTTP response which
could lead to not releasing HTTP connections to the pool and it its eventual
exhaustion.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved algebraic rewriting of query plans which avoid multiple
evaluation of the same part of the query, e.g., the operator in both parts
of a UNION.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; A better handling of the UNION operator by sorting its output
which facilitates joins with other operators in the query plan.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; A better handling of deep chains of nested joins by maintaining
a certain order of intermediate results produced by hash joins and avoiding
page loads.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; An improved, threadsafe implementation of the query plan cache,
which is now concurrently accessible from all connections. This allows
the server to reuse a previously optimized query plan for a structurally
equivalent query executed by another client.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; an init startup script for a Stardog server so that it can
automatically start with other system services at the boot time.

== 0.6.9 Release
[none]
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed an issue pertaining to QL rewrites with optionals that use a
filter which was causing incorrect results for some QL queries (ticket #44).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed query evaluation where the presence of a particular
combintation of union, optional, and distinct can lead to nondistinct result
sets (ticket #48).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed the passing of the disableSecurity flag into the HTTP server
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed a bug in the rendering of SPARQL queries using isBlank with
remote endpoints
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed a bug in query evaluation arising from a certain combination
of order by, limit, and optional where the sort key is on a variable bound
by an optional pattern.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; User specified query bindings specified via Query.parameter are
correctly serialized and sent to the remote server.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Query rewriting now fails when a query has BGP's that are not
supported by the reasoner; this is strict mode.  Strict mode can be disabled
and the reasoner will rewrite only the BGP's it supports.  This introduces a
JVM flag 'strictReasoning' for controlling this behavior.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; HTTP connection will now switch over to HTTP POST if the SPARQL
query is very long to avoid any potential URL overflow issues.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; CLI now includes an 'export' function to dump the contents of part
or all of a Stardog database to RDF.

== 0.6.8 Release
[none]
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Updated POM to include Avro
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Avro schema included in distribution.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed a bug in the caching of the current list of databases, could
still access deleted databases in some circumstances. (ticket #42)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed an bug in the handling of unions in the QL reasoner.  Nested
unions were occasionally getting duplicated leading to incorrect results
without the distinct modifier. (ticket #43, #46)
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better handling of constant values in construct queries
decreasing the time it takes to create and return the Statements constructed
by the query.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Increased the speed of some index scans by avoiding some page
reads
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Better execution plan reuse & caching, similar to Prepared
Statements, in structurally equivalent plans.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Significantly improved join-join cardinality estimations.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Query optimizer does a better job with join selection and can
often pick a better join for the execution.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Avro & HTTP connections are eagerly validated by the driver so
invalid connections will fail on connect rather than on first action.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Transaction ID generation changed to reduce chance of ID
collision
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Internal connection management, both with raw database
connections and HTTP based connections to address deadlock issues in highly
concurrent environments

== 0.6.7 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; HTTP error in construct queries via the Jena bindings
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New CLI command 'server' which can start any of the installed
Stardog servers
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New Avro-based RPC protocol for Stardog Connections
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Maven POM file included in distribution

== 0.6.6 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; incorrect query evaluation results in some corner cases involving
OPTIONAL
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved query evaluation performance with a new query planner
* image:m.png[Fixed,15,16]&nbsp;&nbsp; command-line interface to accept global args: `--home, --change-buffer-size, --disable-security`
* image:m.png[Fixed,15,16]&nbsp;&nbsp; command-line add, remove, create commands to include
`--strict-parsing` option

== 0.6.5 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query evaluation where we were missing results when the
first entry in an iterator in a disk index occurs on a page boundry
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for fulltext search via our semantic search engine Waldo.

== 0.6.2 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Typo in HTTP protocol, Trig & Trix mimetypes were switched.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in left outer joins when the right hand operator was initially
empty causing the first solution set to be missed. (ticket #39)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved IllegalArgumentException which was incorrectly thrown from
the Jena integration when a query w/ optionals was executed against a remote
Stardog database returning null values in the response.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in detection of special BNode syntax in queries for ensuring
BNode stability in queries.

== 0.6.1 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Debugging exception thrown when using log4j bindings for SLF4j with
the logging level set to debug. (ticket #36)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue with calcuation of optionals in joins when the optional
binding is at the beginning or end of an iteration. (ticket #34)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolved synchronization issue for connections to an index that are
fetched from the internal connection pool.  Resolves getting already closed
connections from the pool. (ticket #37)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Datatyped literals used in filters were getting munged during QL
rewrites resulting in an exception. (ticket #35)
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Iteration over disk indices modified to more efficiently
calcuate which pages are actually required to answer the query and avoid
loading intermediate pages.

== 0.6 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug pertaining to loading datasets which use literals larger than
8k.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in the writer locks that could allow multiple writers access to
an index in a multithreaded environment, which usually lead to corruptted
indexes (ticket #29).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue with the CLI's database create command, multiple calls in
rapid succession to create databases caused some of the new databases to be
lost (ticket #32).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; QL query rewriter was mangling the variables used within the query
in certain situations causing query results to be incorrect (ticket #30).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; QL query rewriter was not correctly respecting variable
substitutions with datatyped literals and was subsequently losing the
variable binding specified by the user (ticket #31).
* image:f.png[Fixed,15,15]&nbsp;&nbsp; FROM/FROM Named clauses were incorrectly set to the HTTP server
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Improved index page load speed.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Reduced memory requirements in the indexes wrt to cache overhead
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Persistence of memory indexes to disk is now done in parallel to
the greatest extent possible yield a significant increase in save (and load)
performance.  This required changing the on disk format of memory indexes.
Old versions of memory indexes are unfortunately, backwards incompatible
with these updates.  We apologize for the inconvenience.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; On format of disk-based indexes.  This helps us resolve some
issues we had with the previous format, and increases load speed, but the
index format is backwards incompatible with the previous indexes.  However,
we provide a migration utility in the command line program for changing
old disk indexes to the new format.  See the CLI for more information on
migrating old indexes (or ping the mailing list).
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Decreased memory usage in the query engine
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Added a change buffer in the layer between the SNARL API and
the underlying index it is connected to.  This allows most changes within a
transaction to be pushed into the index at once increasing the performance
of commits as the changes can mostly be applied in bulk.

== 0.5.3 Release
[none]

* image:a.png[Fixed,15,14]&nbsp;&nbsp; Internal buffer for changes via the Sesame API, triples are no
longer added to the Stardog database one at a time via the SAIL API.  They
are buffered until the buffer is full, a read operation occurs, or the
transaction is committed.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Internal bookkeeping for the state of the system information,
timestamp for last update time is correctly updated and you should not get
the out of sync warnings printed to the console.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Incorrect return code for parse errors in HTTP protocol.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE thrown by TBox extractor when encountering non object/data
properties.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Correct behavior for SPARQL queries w/ QL reasoning that uses the
'GRAPH' keyword.  BGP's in graph contexts are not rewritten as inferences
are put into the default, no context, graph.  Hybrid queries that include
some patterns against the default graph and others against patterns in graph
contexts are correctly partially rewritten. (ticket #27)

== 0.5.2 Release
[none]

* image:m.png[Fixed,15,16]&nbsp;&nbsp; Add, Remove and Clear Contexts now take an optional query
parameter, "graph-uri" which specfies the named graph the operation should
be executed against.  This deprecates the use of this parameter *in* the
actual URL.  See HTTP_PROTOCOL.txt for additional details.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Temp files used to track changes during transactions and to
buffer results coming into HTTP server are more aggresively removed.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Specifying 'null' as the context to remove via the Connection API
now actually deletes the default context rather than the entire contents of
the database.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cast exception caused during QL query execution when the usage of
the property in the query does not match the type (object v data) of the
property in the kb. (ticket #24)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NPE during evaluation of a query with QL reasoning enabled when the
query has a optionals nested inside of a graph statement. (ticket #26)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Could get NPE's during filter evaluation (compare expressions only)
if one of the variables in the expression was not bound to any value in a
BGP.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Malformed literals in inserts could come back and cause errors when
being removed.  Errors are now thrown when a malformed literal (invalid
language tag or inconsistent xsd datatype) is inserted into the database.
(ticket #22 & #25)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Resolving BGP's across contexts using wildcards (e.g. graph ?g {
... }) was losing information between the contexts leading to incorrect
results and/or null values in the results. (ticket #20 & #21)
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Gremlin integration via our Sesame Sail implementation and the
Blueprints API.  See the README for more details on the integration.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; statements(subject, predicate, object, context) to the Remover API
and deprecated statement(...); the latter only accepted non-null values,
while the former does accept nulls as wildcards, so it is safe to replace
all usages of one with the other. statement(...) is marked as deprecated and
schedule for removal from the API no earler than 0.6.

== 0.5.1 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Deleting statements with wildcards via a StardogRepository.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Support for the Sesame RepositoryFactory & Config APIs so Stardog
can be used via the OpenRDF shell and Workbench.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Native connection pools for Stardog connections.  See the examples
for code demonstrating how to configure and use them.

== 0.5 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query objects reference to database was not refeshed after commit
or rollback of transaction. (ticket #15)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in queries with graph contexts using unbound variables via an
HTTP connection; the variable was getting mangled.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Introduced new Flex Indexes into the Database structure.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; Shiro based security layer

== 0.4.10 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in the concurrent calcuation of statistics where a latch could
get stuck when certain types of exceptions occured during processing
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue in Remove CLI where description did not match behavior;
Specifying only a context now deletes the context as stated in the
documentation. (ticket #17)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in removing a context within a durable transaction that caused
the transaction record to get corrupted. (ticket #16)
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Changed Add CLI to print an error message if no files are
specified to add to the database.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Removed default parse format of RDF/XML from add & remove
operations.  If a specific format is specified, that is used, otherwise the
format based on the file extension will be used.  This brings the behavior
of add & remove inline with create.  RDF/XML is still used as the fallback
when no format is specified and there is a nonstandard extension on the
file.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Modified how some filters are evaluated greatly increasing the
speed in which basic (and/or/not & compares) filters are executed.

== 0.4.9 Release
[none]

* image:a.png[Fixed,15,14]&nbsp;&nbsp; Handling of the TOP concept in the OWL 2 QL reasoner
* image:a.png[Fixed,15,14]&nbsp;&nbsp; New query operator which provides more efficient evaluation of
queries where order by and limit are used.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; System information regarding which databases are present will be
reloaded from disk when it is changed by an external source.
* image:a.png[Fixed,15,14]&nbsp;&nbsp; JVM flag to disable strict mode of parsing.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Encoding of graph URIs in remove context calls to HTTP server.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Typo in route which effectively hid the remove context endpoint in
the HTTP server.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Mangling of ASK queries when sending them to the HTTP server.
(ticket #13)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Typo in help information in the add/remove data commands in the
CLI.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; BOM markers at the start of files will be gracefully handled
instead of failing to parse.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; If copy of system info on disk is more recent than cached copy in
memory, it will not be overwritten on shutdown. (ticket #11)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; UnsupportedOperationException thrown when clearing a context within
a transaction. (ticket #12)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; IllegalArgumentException thrown when literals are used in query
atoms going through rewriting. (ticket #9)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; ClassCastException when non-QL axioms are used. (ticket #8)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Issue with deleting directories when STARDOG_HOME contains relative
paths. (ticket #6)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; NullPointerException when top level scope contains an empty set of
query atoms. (ticket #7)
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Closing temporary files in the data directory used for some file
operations.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Concurrent modification exception from an iterator when persisting
memory indexes to disk asynchronously.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; HTTP protocol was dropping context information from queries.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Queueing of jobs during concurrent loading to avoid running out
of memory when processing large datasets. This also reduces the amount of
memory used during loading.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Altered algorithm to get better estimates of join cardinalities.
* image:m.png[Fixed,15,16]&nbsp;&nbsp; Memory databases to use new statistics algorithm.

== 0.4.8 Release
[none]

* image:a.png[Fixed,15,14]&nbsp;&nbsp; Creation of db indices is done in parallel. It is an experimental
feature which can be disabled via the JVM flag (disableConcurrent); note,
that concurrent loading requires more RAM than serial loading. Either give
Stardog more RAM (i.e., as much as you cann spare) on startup or pass the
disableConcurrent flag.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Query atoms of the form { ?a ?a ?b } now return correct results
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in multi-key joins
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in QL reasoning optimizer for detecting empty joins
* image:f.png[Fixed,15,15]&nbsp;&nbsp; File exhaustion bug.  When Sesame integration was used with the
HTTP server, the server did not always close the temporary files used to
track the incoming edits causing some systems to report too many files were
open.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Bug in query evaluation where the named graph bound in a pattern
could cause an exception during execution.
* image:f.png[Fixed,15,15]&nbsp;&nbsp; Cardinality estimates in index statistics
* image:f.png[Fixed,15,15]&nbsp;&nbsp; bug in stardog.sh script so that it can run anywhere (thanks Alin Dreghiciu for this fix)
* image:m.png[Fixed,15,16]&nbsp;&nbsp; The name of the file being added/removed from a database in the command line is now printed.

== 0.4.7 Release
[none]

* image:f.png[Fixed,15,15]&nbsp;&nbsp; Fixed an NPE in the HTTP server (reported by Brian Sletten)
* image:m.png[Fixed,15,16]&nbsp;&nbsp; make the QL axioms parser more strictly respect the OWL spec, especially w/r/t to the set of datatypes that QL supports
